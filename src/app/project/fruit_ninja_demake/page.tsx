import { BottomBar, SectionTitle } from "@/components";
import { projects } from "@/data/projects";

export default function FruitNinjaDemake() {
  return (
    <section className="p-6">
      <BottomBar />
    </section>
  );
}